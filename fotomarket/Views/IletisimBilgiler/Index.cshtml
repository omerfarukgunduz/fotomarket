@using fotomarket.Models
@model List<Table_IletisimBilgileri>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_adminLayout.cshtml";
}


<div style="text-align: center;">
    <h2>İletişim Bilgileri Düzenleme Sayfası</h2>
</div>

<div class="container">
    <div class="table-responsive">
        <table class="table table-bordered">
            <tbody>
                @foreach (var x in Model)
                {
                    <tr>
                        <th scope="row">ID</th>
                        <td>@x.ID</td>
                    </tr>
                    <tr>
                        <th scope="row">Düzenleyen Kullanıcı ID</th>
                        <td>@x.KullaniciID</td>
                    </tr>
                    <tr>
                        <th scope="row">Telefon Numarası</th>
                        <td>@x.TelefonNumarasi</td>
                    </tr>
                    <tr>
                        <th scope="row">E-posta Adresi</th>
                        <td>@x.EpostaAdresi</td>
                    </tr>
                    <tr>
                        <th scope="row">Güncelle</th>
                        <td><a href="/IletisimBilgiler/IletisimBilgileriGetir/@x.ID" class="btn btn-warning">Güncelle</a></td>
                    </tr>

                }
            </tbody>
        </table>
    </div>
</div>
<br />

